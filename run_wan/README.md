# run_wan

Wan 推理脚本目录（Jano + baseline）：

- `jano_generate.py`
- `pab_generate.py`
- `teacache_generate.py`
- `toca_generate.py`

推荐统一入口（仓库根目录）：

```bash
python launch.py --model wan --method jano
python launch.py --model wan --method pab
```

